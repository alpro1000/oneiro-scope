"""Billing / monetization layer (Phase 6).

- `quotas` — tier-based usage limits and the `assert_quota` enforcement.
- `lemon_provider` — Lemon Squeezy (Merchant of Record) integration:
  checkout creation, webhook signature verification, subscription lifecycle.
"""
