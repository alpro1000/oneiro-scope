# FastAPI & ASGI
fastapi==0.109.0
# >=0.31.1 required by mcp (its stdio/HTTP transports run on uvicorn).
uvicorn[standard]>=0.31.1,<1
# Post-2.5 (avoids the recursion issue in field __repr__) and >=2.11 because
# claude-agent-sdk needs mcp>=1.23, which requires pydantic>=2.11. fastapi
# 0.109 accepts any pydantic <3, so the floor is safe to raise.
pydantic>=2.11,<3
# >=2.6.1 required by mcp; it only needs pydantic>=2.7.0, so the pydantic
# pin above still holds. Kept <3 so a major bump can't land unreviewed.
pydantic-settings>=2.6.1,<3

# Database
sqlalchemy==2.0.25
alembic==1.13.1
psycopg2-binary==2.9.9
asyncpg==0.29.0

# Redis
redis==5.0.1
hiredis==2.3.2

# Celery (background tasks)
celery==5.3.6
celery[redis]==5.3.6

# Authentication
python-jose[cryptography]==3.3.0
email-validator>=2.0  # Pydantic EmailStr
passlib[bcrypt]==1.7.4
# >=0.0.9 required by mcp (form parsing for its HTTP transport).
python-multipart>=0.0.9,<0.1

# LLM & AI
openai==1.10.0
anthropic==0.8.1
tiktoken==0.5.2
# Cloud LLM gateways (optional — only needed if VERTEX_* or AWS Bedrock
# credentials are configured; providers gracefully disable when absent).
google-auth>=2.27
boto3>=1.34

# ASR
# openai-whisper removed: nothing in backend/ imports it, its sdist fails to
# build (setup.py imports pkg_resources, gone from current setuptools) and it
# drags in torch. Re-add with a maintained wheel if server-side ASR is ever
# wired up — speech input is handled client-side by the Web Speech API today.
vosk==0.3.45

# Lunar calculations
pyswisseph==2.10.3.2
geopy==2.4.1
timezonefinder==6.5.5
ephem==4.1.5

# NLP
spacy==3.7.2

# Dream Analysis
-e ./external/DReAMy
pandas>=2.0.0

# Vector DB
pgvector==0.2.4

# HTTP Client
# httpx must be >=0.27 for `mcp[cli]`, but <0.28 because httpx 0.28 removed the
# deprecated `proxies=` kwarg that openai==1.10.0 / anthropic==0.8.1 still pass.
httpx>=0.27,<0.28
aiohttp==3.9.1

# Payment
stripe==7.11.0

# Monitoring
prometheus-client==0.19.0
sentry-sdk[fastapi]==1.40.0

# MCP server + Claude Agent SDK
# MCP exposes the OneiroScope services as tools for skills/agents.
# Floor set by claude-agent-sdk (needs mcp>=1.23); mcp in turn drives the
# pydantic>=2.11 floor above. A floor this explicit also keeps pip from
# backtracking through dozens of releases, which slows the Render build.
# No [cli] extra: it only adds typer for the `mcp` command-line helper, which
# nothing here uses (the server runs via `python -m backend.mcp.server` on
# FastMCP from core mcp), and its typer>=0.16 collides with spacy's typer<0.10.
mcp>=1.23
claude-agent-sdk>=0.2

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0

# Utils
python-dateutil==2.8.2
pytz==2023.3
pyyaml==6.0.1
python-dotenv==1.0.0
tenacity==8.2.3
pandas==2.2.2
mne==1.7.0

# Development
ruff==0.1.14
black==23.12.1
mypy==1.8.0

# Physiognomy: server-side photo landmarks (optional heavy CV path;
# /physiognomy/analyze-photo returns 501 without these)
mediapipe==0.10.14
opencv-python-headless>=4.9,<5.1
