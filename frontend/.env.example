# Shared runtime configuration for the Next.js frontend
# Copy this file to `.env.local` (local dev) or set the variables in Render.

# URL of the lunar API. Leave empty to use the built-in Next.js mock route.
LUNAR_API_URL=
NEXT_PUBLIC_LUNAR_API_URL=

# Default timezone that will be used when the client does not send `tz`.
# For Russian-speaking users, use Europe/Moscow to match local lunar calendars
LUNAR_DEFAULT_TZ=Europe/Moscow

# Required by the chat assistant on the backend. Render: add under Environment Variables.
OPENAI_API_KEY=sk-your-openai-api-key

# Production builds should set this to "production".
NODE_ENV=development
