[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "pyswisseph"
version = "0.1.0"
description = "Lightweight Python stub for Swiss Ephemeris bindings"
requires-python = ">=3.10"
