[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "DReAMy"
version = "0.1.0"
description = "Lightweight stand-in for DReAMy embedding toolkit"
requires-python = ">=3.10"
