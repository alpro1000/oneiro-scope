{
  "name": "@oneiroscope/chart-kit",
  "version": "0.1.0",
  "private": true,
  "description": "Pure client-side chart geometry derived from a chart_core payload — angles, houses, aspects, dignities and astrocartography with no network and no ephemeris.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": ["src"],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "golden": "python3 ../../scripts/generate_chart_golden.py"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
